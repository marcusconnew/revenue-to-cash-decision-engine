# Week 6 Capstone — Phase 1 deployment proof
# Floors only at this stage. Once T5.1 passes, replace with the exact
# versions reported on the Technical assurance tab. That pinned set is the
# reproducibility baseline supporting acceptance test T3.6.

streamlit==1.61.1
pandas==2.3.3
numpy==2.5.2
statsmodels==0.14.6
scikit-learn==1.9.0
xgboost==3.4.1
plotly==6.9.0
openpyxl==3.1.5
